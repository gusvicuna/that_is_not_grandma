namespace Game.Domain
{
    /// <summary>Values are used as array indices in MusicLayerMixer: keep them contiguous from 0.</summary>
    public enum MusicLayerId
    {
        Bed,
        Approach,
        Lie,
    }
}
